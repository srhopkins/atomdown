// Package atomdown parses and validates persistent block IDs, ordered groups,
// and extensible XML-shaped metadata embedded in ordinary Markdown comments.
// Atomdown documents remain valid CommonMark and visible Markdown remains the
// source of truth.
package atomdown
